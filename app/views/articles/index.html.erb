<h1>HackedNews</h1>
<div class="articles">
  <% @articles.each do |article| %>
  <div class="title">
    <%= link_to image_tag('votearrow.gif'), votes_path(value: 1), method: :post %>
    <%= link_to article.title, article.url %>
  </div>
  <% end %>
</div>
<br><br>
<%= link_to "Submit an Article", new_article_path, class: "btn" %>